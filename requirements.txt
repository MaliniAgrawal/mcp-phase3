# MCP + runtime
fastmcp==0.2.0
pydantic==2.12.4
loguru==0.7.3

# AWS SDK
boto3==1.40.60
botocore==1.40.60

# HTTP Adapter
fastapi==0.48.0
uvicorn==0.38.0

# NLP (local + optional vector/RAG)
transformers==4.57.1
torch==2.9.0+cpu
regex==2025.11.3
faiss-cpu==1.7.4  # optional; only required for RAG

# Anthropic Haiku fallback
anthropic==1.1.4

# Utilities
python-dotenv==1.2.1
python-json-logger==2.0.4  # optional if you want built-in JSON formatting elsewhere
